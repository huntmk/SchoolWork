#ifndef MASKING_H
#define MASKING_H

#include <vector>
#include <string>

int scorePattern(int**, int);
int getLength(int);
std::vector<int> getBinary(int, int);
void genMaskingPatterns(int**, int, std::string);

#endif
